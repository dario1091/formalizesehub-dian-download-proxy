{
  "name": "@formalizesehub/dian-download-proxy",
  "version": "1.0.0",
  "description": "Lambda proxy: POST /dian/download → SQS",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.700.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.3.0"
  }
}
